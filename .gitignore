# This repo holds a single action.yml plus its README — no build, no dependencies,
# no virtualenv. Deliberately short rather than the Python .gitignore the app repos
# carry: nothing here produces artefacts.

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# Local secrets, if anything is ever tested against a real environment
.env
